-- Historical marker.
--
-- This migration previously embedded a 90 MB mixed data dump (catalog,
-- staging references and user profiles). It was not reproducible on a fresh
-- database because parent rows and staging dependencies were split across
-- unversioned files. Runtime catalog data now lives outside Git in Expanse as
-- a custom pg_dump generated by scripts/backup-runtime-catalog.mjs and loaded
-- after the production-contract migrations with scripts/seed-catalog.sh.
--
-- The remote migration ledger retains this version because it was already
-- applied historically. No schema change is required here.
